{
  "name": "nexora-extractor",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "render-build": "pup-install && node server.js",
    "build": "npx puppeteer browsers install chrome",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "puppeteer": "^22.6.0"
  }
}
